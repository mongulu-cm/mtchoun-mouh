tasks:
  - init: |      
      pip install -r requirements.txt && pip install -r api/requirements.txt
      pre-commit install
    command: |
      bash .envrc.gitpod

vscode:
  extensions:
    - vivaxy.vscode-conventional-commits